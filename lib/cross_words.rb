class CrossWords
    def self.intersect(horizontal:, vertical:)
        # Iterate through the horizontal character finding each character with its index
        horizontal.each_char.with_index do |h_char, h_idx|
            
        end
    end
end